<!--
  Apply to android/app/src/main/AndroidManifest.xml after platform add:

  <application
      android:usesCleartextTraffic="false"
      android:networkSecurityConfig="@xml/network_security_config"
      ... >

  Intent filter allowlist (App Links — enable autoVerify only after assetlinks.json):

  <intent-filter android:autoVerify="false">
      <action android:name="android.intent.action.VIEW" />
      <category android:name="android.intent.category.DEFAULT" />
      <category android:name="android.intent.category.BROWSABLE" />
      <data android:scheme="https" android:host="interstitiumlabs.dev" />
  </intent-filter>
-->
